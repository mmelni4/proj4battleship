// Enumerator class for ship statuses, when ships are placed on the grid,
// default status is untouched, then changed accrodingly to the action

package UI;

public enum Status {
	EMPTY, UNTOUCHED, HIT, MISS
}
